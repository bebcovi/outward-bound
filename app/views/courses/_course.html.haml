%h1.course_title= smarty_pants course.name

- if course.album
  .course_cover
    = link_to(course.album) do
      = image_tag(course.cover_photo.url(:medium), alt: course.cover_photo.title)

= markdown course.description

.course_info.gw
  - if course.dates?
    .dates.g.one-whole.lap-one-half.desk-one-fifth
      = icon("calendar")
      .name= t(".dates")
      .value= markdown course.dates

  - if course.duration?
    .duration.g.one-whole.lap-one-half.desk-one-fifth
      = icon("clock")
      .name= t(".duration")
      .value= days course.duration

  - if course.min_people? or course.max_people?
    .people.g.one-whole.lap-one-half.desk-one-fifth
      = icon("users")
      .name= t(".people")
      .value= "#{course.min_people}–#{course.max_people} #{t("misc.people")}"

  - if course.price?
    .price.g.one-whole.lap-one-half.desk-one-fifth
      = icon("coins")
      .name= t(".price")
      .value= money course.price

  - if course.contact?
    .price.g.one-whole.lap-one-half.desk-one-fifth
    .name= t(".contact")
    .value= markdown course.contact

- if course.applyable?
  .apply_for_course
    = link_to t("misc.apply").append_icon("file-download"), apply_courses_path, class: "application_form"
    %p.send_application_form
      = t(".send_application_form")
      = mail_to("apply@outwardbound.hr") + "."
