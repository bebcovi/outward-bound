= simple_form_for @email_forward, url: [:admin, @email_forward] do |f|

  %fieldset
    = f.input :from, label: false, wrapper: false
    .suffix @outwardbound.hr
    .arrow= icon("arrow-right")
    = f.input :to, label: false, wrapper: false
    = f.button :button, "Dodaj", class: "btn-primary"
